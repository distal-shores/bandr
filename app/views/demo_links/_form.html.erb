<%= form_for(@demo_link) do |f| %>
  <% if @demo_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demo_link.errors.count, "error") %> prohibited this demo_link from being saved:</h2>

      <ul>
      <% @demo_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
