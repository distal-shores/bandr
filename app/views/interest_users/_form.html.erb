<%= form_for(@interest_user) do |f| %>
  <% if @interest_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest_user.errors.count, "error") %> prohibited this interest_user from being saved:</h2>

      <ul>
      <% @interest_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
